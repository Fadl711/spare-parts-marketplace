# 🔧 FIX GUIDE - حل مشكلة الاتصال

## المشكلة التي عندك:

1. ❌ **MOCK_VEHICLES doesn't exist** - التطبيق لم يحدّث
2. ❌ **Network Error** - الجوال ما يقدر يوصل للـ Backend

---

## ✅ الحل (خطوة بخطوة):

### الخطوة 1: اعرف IP الجهاز

**افتح PowerShell** واكتب:

```powershell
ipconfig
```

ابحث عن **IPv4 Address**، مثل:

```
Wireless LAN adapter Wi-Fi:
   IPv4 Address. . . . . . . . . . . : 192.168.1.5
```

---

### الخطوة 2: عدّل IP في الكود

افتح: `src/services/api.ts`

**السطر 7** غيره من:

```typescript
const YOUR_COMPUTER_IP = "192.168.1.5"; // 👈 CHANGE THIS!
```

إلى IP جهازك (اللي طلع معك من ipconfig):

```typescript
const YOUR_COMPUTER_IP = "192.168.X.X"; // ضع IP جهازك هنا
```

---

### الخطوة 3: شغّل Backend على الشبكة

**أوقف** `php artisan serve` الحالي (Ctrl+C)

ثم شغله بهذا الأمر:

```bash
php artisan serve --host=0.0.0.0
```

هذا يخلي Backend يقبل اتصالات من أي جهاز على نفس الشبكة.

---

### الخطوة 4: أعد تشغيل التطبيق

في terminal الـ Expo:

1. اضغط `Ctrl+C` لإيقاف Metro
2. شغله من جديد:

```bash
npm start
```

أو في **التطبيق على الجوال**:

-   اضغط `R` (Reload) للتحديث

---

## ✅ اختبر الحل:

1. افتح المتصفح واذهب إلى:

```
http://192.168.X.X:8000/api/v1/parts
```

(ضع IP جهازك بدل X.X)

2. لو طلع JSON = Backend شغال ✅
3. افتح التطبيق على الجوال
4. لو ظهرت البيانات = تمام! ✅

---

## ⚠️ ملاحظة مهمة:

**تأكد أن الجوال والجهاز على نفس الشبكة!**

-   نفس الـ Wi-Fi
-   ما تستخدم Mobile Data
-   ما فيه Firewall يمنع الاتصال

---

## 🔍 إذا ما زال فيه مشكلة:

1. تأكد IP صح (ipconfig)
2. تأكد Backend شغال (php artisan serve --host=0.0.0.0)
3. تأكد من نفس الشبكة
4. جرب في المتصفح أولاً
5. شيك Firewall (قد يحجب Port 8000)

---

**بعد ما تسوي هذي الخطوات، جرب التطبيق وخبرني!** 🚀
